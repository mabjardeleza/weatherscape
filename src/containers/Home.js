import React from 'react';
import { Header, Forecasts, Settings } from '../components';

const Home = () => (
  <div>
    <Header />
    <Settings />
    <Forecasts />
  </div>
);

export default Home;
